{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then cd ../../packages/backend && bunx convex deploy --cmd 'cd ../../apps/web && bun run build'; else cd ../.. && bun run build; fi",
  "framework": "nextjs",
  "installCommand": "bun install"
}
